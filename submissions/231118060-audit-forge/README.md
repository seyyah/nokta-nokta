Track: A

# Nokta Audit-Forge Submission

Student: Ali Ordek  
Student no: 231118060  
Branch: `codex/231118060-audit-forge`  
Submission path: `submissions/231118060-audit-forge`

## 1. Project Overview

This submission keeps the previous Audit-Forge app and turns it into a live audit cockpit. The user can move between Capture, Reports, Forge and Bridge routes while one audit widget remains mounted at the root. The new work adds real microphone metering, a GLB analyst avatar, amplitude-driven mouth response and a human-in-the-loop bridge for cases that get stuck.

The product tone is intentionally quiet: no neon dashboard, no fake call screen, no random waveform. The live panel reads the device microphone, smooths the metering value and uses the same signal for both the bars and avatar mouth.

## 2. Existing Baseline From Previous Week

The baseline already had:

- Expo + TypeScript + Expo Router under `app/`
- Three routes: `/`, `/reports`, `/forge`
- `@xtatistix/mobile-audit` mounted once in `app/app/_layout.tsx`
- host-provided audit deps for capture, storage, file write and share
- three Markdown audit reports with burn-in evidence
- `FORGE.md`, `EVAL.md`, `IDEA.md`
- a release APK and a 60 second demo artifact

Those boundaries are preserved. The audit widget still lives in the root layout; route files only pass a screen key into the shared screen component.

## 3. What Changed This Week

- Added `/bridge` as a fourth route without moving the root audit mount.
- Added real microphone permission and metering through `expo-av`.
- Added smoothed voice bars driven by recorded mic `metering` values.
- Added `app/assets/avatar.glb`, generated as a local low-poly analyst avatar asset with a named `Mouth` node.
- Added Three/GL rendering with `expo-gl`, `expo-three`, `three` and `GLTFLoader`.
- Linked mic amplitude to avatar mouth scale and subtle head response.
- Added Jitsi Meet external bridge for audio, video and screen share.
- Added new audit evidence for voice/avatar, report continuity and bridge escalation.
- Added engineering docs: `DECISIONS.md`, `BRIDGE.md`, `VERIFY.md`.
- Hardened the live path so mic startup, avatar GLB load and bridge URL failures report honestly instead of failing silently.

## 4. Architecture Summary

The app remains a small Expo Router surface:

- `app/app/_layout.tsx`: root shell, audit deps and single `AuditWidget`
- `app/app/index.tsx`: Capture route
- `app/app/reports.tsx`: Reports route
- `app/app/forge.tsx`: Forge route
- `app/app/bridge.tsx`: human bridge route
- `app/src/NoktaScreen.tsx`: shared screen frame
- `app/src/screens.ts`: route metadata
- `app/src/audio/useMicrophoneLevel.ts`: mic permission and metering hook
- `app/src/components/VoiceAvatarPanel.tsx`: live voice surface
- `app/src/components/AvatarStage.tsx`: GLB avatar renderer
- `app/src/components/BridgePanel.tsx`: escalation bridge launcher

## 5. Voice Pipeline

`useMicrophoneLevel` requests microphone permission on demand. When permission is granted, `Audio.Recording.createAsync` starts a real recording session using Expo AV's high quality preset with metering enabled. The status callback runs every 80ms and reads `status.metering`.

The dB value is mapped from a floor/ceiling range into `0..1`. An attack/release smoothing step reacts faster to rising speech and slower to falling silence. Silence therefore returns to an idle state instead of jittering. The recording options explicitly keep `isMeteringEnabled: true` even though the Expo preset already includes it, so the requirement is visible in code review.

## 6. Avatar Pipeline

The avatar is stored at:

`app/assets/avatar.glb`

It is generated by `app/scripts/create-avatar-asset.mjs`. The GLB contains a stylized analyst bust, material names and a named `Mouth` node. This is not a robot placeholder and not a future TODO asset; it is the asset loaded by the app.

`AvatarStage` loads the GLB with `Asset.fromModule` and `GLTFLoader`, renders it in `GLView`, and keeps references to `Mouth` and `AnalystHead`.

## 7. Lipsync Strategy

This build uses amplitude-driven lipsync. It does not claim phoneme or viseme recognition. The mouth opens from live microphone amplitude:

- idle/silence: mouth scale returns close to `1`
- speech: mouth scale increases on the Y axis
- stronger speech: mouth opens wider and the head leans slightly

That is enough for a visible and honest mouth response without pretending to understand words.

## 8. Audit System

The audit integration is preserved:

- `AuditWidget` appears once in `app/app/_layout.tsx`
- `currentScreen` is derived from `usePathname()`
- capture and file abilities stay in host deps
- AsyncStorage remains the note store
- reports stay inside `audit-reports/`

New reports:

- `audit-reports/voice-avatar-lipsync.md`
- `audit-reports/audit-continuity.md`
- `audit-reports/bridge-escalation.md`

Baseline reports are kept as historical audit evidence.

## 9. Forge Workflow

`FORGE.md` now records the previous baseline cycles plus this week's cycles: sparse checkout recovery, voice/avatar implementation and documentation/evidence writeback. The rollback/stuck case is real: full checkout timed out, so the work moved to sparse checkout for only this submission path.

## 10. HITL Bridge

The bridge uses Jitsi Meet:

`https://meet.jit.si/nokta-audit-forge-231118060`

This choice avoids fake native call UI. The app opens the real meeting target through `Linking.openURL`. Audio, video and screen share are provided by Jitsi itself. The app presents the escalation packet context so the expert receives the same audit case framing.

## 11. Verification Steps

Run from `submissions/231118060-audit-forge/app`:

```bash
npm install
npm run typecheck
npx expo install --check
npx expo export --platform android
```

From repo root:

```bash
rg -n "AuditWidget" submissions/231118060-audit-forge/app -g "!node_modules"
git diff --name-only origin/codex/231118060-audit-forge
```

## 12. Demo Instructions

Start the app:

```bash
cd submissions/231118060-audit-forge/app
npx expo start --tunnel
```

Flow:

1. Open Capture.
2. Tap `Enable microphone` and grant permission.
3. Speak; the bars and avatar mouth should respond.
4. Visit Reports and Forge to confirm audit continuity.
5. Open Bridge and launch the Jitsi expert room.

## 13. APK Usage

`app-release.apk` is the release artifact for this submission. The earlier native build was produced from an ASCII temp path because the Android NDK/CMake toolchain failed on the Turkish-character workspace path.

After the resumed JS hardening pass, a full Gradle rebuild was attempted again but the local Gradle/Prefab cache blocked it. Because the native shell and native dependencies did not change, the APK was refreshed by embedding the current Hermes `assets/index.android.bundle` into the existing release shell, then running `zipalign` and `apksigner verify`. The resulting APK contains `AndroidManifest.xml`, `classes.dex`, the updated Hermes bundle and the bundled GLB asset.

## 14. AI Tool Usage Log

- Codex: repo discovery, sparse checkout recovery, implementation, docs, verification commands.
- GitHub plugin: branch/repo orientation and remote file inspection.
- Local toolchain: npm install, Expo package install, TypeScript, Expo export, Gradle release APK build, grep checks, generated GLB and burn-in evidence.

## 15. Human Touch Points

1. The student identity, repo and assignment constraints came from the user.
2. The user approved moving from discovery into implementation.
3. Bridge provider choice remained engineering-driven: stable external Jitsi flow over fake native UI.

## 16. Known Limitations

- Lipsync is amplitude-based, not phoneme-level.
- Jitsi is launched externally; it is a real bridge, but not embedded in a native WebView.
- Expo GLB loading should be verified on a physical Android device because emulator/browser behavior can differ.
- The demo artifact is an evidence walkthrough, not a phone screen recording, unless a device recording is produced later.
- The latest APK was JS-bundle refreshed and re-signed after full Gradle rebuild hit a local Prefab/toolchain issue; native code was not changed in that hardening pass.

## 17. Next Cleanup Opportunities

- Replace amplitude lipsync with a lightweight viseme classifier if speech recognition becomes part of the stack.
- Move bridge packet handoff into a generated Markdown case file.
- Add a device-recorded demo after physical Android verification.
